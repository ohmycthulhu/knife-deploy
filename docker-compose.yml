version: "3.4"

services:
  api:
    build: ../api/.
    environment:
      - DB_USER=user
      - DB_PASSWORD=s#C%2j6x4&
      - DB_HOST=192.168.1.128
      - DB_CONNECTION=mysql
      - DB_DATABASE=knife
      - HOST=0.0.0.0
      - MINIMAL_TRIES=-100
    networks:
      - default
  socket:
    build: ../socket/.
    environment:
      - API_URL=http://api:3333
      - MINIMAL_TRIES=-100
    networks:
      - default
    ports:
      - "3210:3000"

networks:
  default:
